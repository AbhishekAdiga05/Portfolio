
  # Build as per prompt

  This is a code bundle for Build as per prompt. The original project is available at https://www.figma.com/design/e7Q7XSnMkAvMBok4Wr30Ku/Build-as-per-prompt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  